# IMPLEMENTATION_PLAN

## 1. 目的

`collage-it` は、9 枚の写真を 3 x 3 のグリッドで 1 枚の正方形画像に結合する Go 製のコマンドラインアプリケーションとする。

## 2. 要件整理

### 機能要件

- 入力として 9 枚の画像ファイルを受け取る
- 各画像は中央部分を基準に 1:1 にトリミングする
- トリミング後の 9 枚を 3 x 3 で結合する
- 出力画像は 1:1 のアスペクト比にする
- 出力ファイルを生成する

### CLI オプション

- `-name-prefix`
  - 対象画像ファイル名のプレフィックスを指定する
  - 指定されたプレフィックスに一致する画像を探索対象にする
- `-spacing-px`
  - 各写真間の余白ピクセル数を指定する
  - 未指定時は妥当なデフォルト値を使用する
- `-artifact-size-px`
  - 生成物の一辺サイズをピクセルで指定する
  - 未指定時は妥当なデフォルト値を使用する

### 非機能要件

- Go の標準的な CLI として単一バイナリで実行できること
- 不正な入力に対して分かりやすいエラーメッセージを返すこと
- 同じ入力とオプションに対して再現性のある出力を生成すること

## 3. 想定ユースケース

1. ユーザーが 9 枚の画像を同一ディレクトリに配置する
2. ユーザーが `collage-it` を実行する
3. アプリケーションが対象画像を収集し、9 枚であることを検証する
4. 各画像を中央トリミングして正方形化する
5. 3 x 3 に配置して 1 枚の正方形画像として書き出す

## 4. 入出力設計

### 入力

- カレントディレクトリまたは指定方針に従って探索された画像ファイル
- 対応形式は初期実装では一般的な形式に限定する
  - 例: JPEG, PNG

### 出力

- 正方形のコラージュ画像 1 枚
- 出力ファイル名は固定値または規則的な命名にする
  - 例: `collage.jpg` または `collage.png`

## 5. 処理フロー

### 5.1 対象画像の収集

- 指定プレフィックスに一致するファイルを列挙する
- 対応拡張子のみ対象にする
- 安定した出力のため、ファイル名でソートする
- 対象が 9 枚ちょうどでない場合はエラーにする

### 5.2 画像の読み込み

- 各画像をデコードする
- 読み込み失敗時は対象ファイル名を含むエラーを返す

### 5.3 中央トリミング

- 元画像の短辺を基準に正方形領域を決定する
- 横長画像は左右を均等に切り落とす
- 縦長画像は上下を均等に切り落とす
- 既に正方形の場合はそのまま扱う

### 5.4 レイアウト計算

- 全体サイズは `artifact-size-px` を使用する
- 余白合計を差し引いた描画可能領域を 3 分割する
- 各セルサイズを算出する
- 端数処理方針を事前に決め、最終画像サイズが常に指定値になるようにする

### 5.5 リサイズと描画

- 各トリミング画像をセルサイズへリサイズする
- 背景色を決めたキャンバスを生成する
- 3 x 3 の各位置へ順番に描画する

### 5.6 出力

- 最終画像をエンコードして保存する
- 書き込み先に失敗した場合は終了コード付きで失敗させる

## 6. CLI 仕様案

### コマンド例

```bash
collage-it -name-prefix trip_ -spacing-px 16 -artifact-size-px 2048
```

### オプション仕様

- `-name-prefix string`
  - デフォルトは空文字
  - 空文字時は対象拡張子の全画像を候補にする
- `-spacing-px int`
  - 0 以上のみ許可する
- `-artifact-size-px int`
  - 3 セルと余白を十分に配置できる最小値以上のみ許可する

## 7. パッケージ構成案

- `cmd/collage-it`
  - エントリーポイント
  - フラグ解析
  - エラーハンドリング
- `internal/files`
  - 画像ファイル探索
  - 入力検証
- `internal/imageops`
  - トリミング
  - リサイズ
  - キャンバス描画
- `internal/collage`
  - レイアウト計算
  - 9 枚の統合処理

初期段階では小規模実装として単一パッケージから開始してもよいが、責務分離しやすい構造を前提とする。

## 8. バリデーション方針

- 対象画像枚数が 9 枚であること
- `spacing-px` が負数でないこと
- `artifact-size-px` が最小必要サイズを満たすこと
- 同名や非画像ファイル混在時に安定して動作すること
- 画像サイズや縦横比がばらばらでも処理できること

## 9. エラーハンドリング方針

- ユーザー起因の入力不備は短く明確なメッセージを返す
- 失敗原因に対象ファイル名やオプション名を含める
- 正常終了は終了コード 0、異常終了は 0 以外を返す

## 10. テスト方針

### 単体テスト

- 中央トリミング範囲の計算
- 3 x 3 レイアウト座標の計算
- オプション値検証
- 対象ファイル選別とソート

### 結合テスト

- 9 枚の画像から正方形画像が生成されること
- `spacing-px` 変更が出力に反映されること
- `artifact-size-px` 変更が出力サイズに反映されること
- 枚数不足や過多で適切に失敗すること

## 11. 実装順序案

1. CLI の引数仕様を確定する
2. 画像ファイル探索と 9 枚検証を実装する
3. 中央トリミング処理を実装する
4. レイアウト計算とキャンバス生成を実装する
5. リサイズと描画を実装する
6. 出力処理を実装する
7. テストを追加する
8. README に使い方を追記する

## 12. 検討事項

- 出力形式を JPEG 固定にするか、PNG を選択可能にするか
- 対象ディレクトリをオプションで受け取るか
- 背景色を固定にするか、将来的にオプション化するか
- EXIF の向き補正を初期実装で扱うか
