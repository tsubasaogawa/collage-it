# collage-it

## Usage

```bash
go run ./cmd/collage-it -name-prefix trip_ -spacing-px 16 -artifact-size-px 2048
```

### Flags

- `-name-prefix`: input image filename prefix
- `-spacing-px`: spacing between photos in pixels
- `-artifact-size-px`: output image side length in pixels